/**
 * Оставлено для обратной совместимости — вся логика теперь в providers.ts,
 * который умеет работать с несколькими провайдерами (OpenRouter, GigaChat,
 * OpenAI, Anthropic, DeepSeek, Google), а не только с OpenRouter.
 */
export * from "./providers";
